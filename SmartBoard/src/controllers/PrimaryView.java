package controllers;

public interface PrimaryView { 
	// This interface is used to limit which FXML files can be viewed after adding to primaryStage.
	// PrimaryViews include: LoginController, SignUpController, WorkspaceController
}
